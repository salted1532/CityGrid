using UnityEngine;

public class EventManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //각 이벤트들을 받아 다른 시스템, 매니저들에게 전달

}
